{
  "name": "anyon-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd landing && npm run dev",
    "build": "cd landing && npm run build",
    "preview": "cd landing && npm run preview",
    "install:all": "npm install && cd landing && npm install"
  },
  "devDependencies": {
    "@vercel/node": "^5.5.16",
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "resend": "^6.6.0"
  }
}
