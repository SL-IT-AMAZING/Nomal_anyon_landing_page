{
  "buildCommand": "npm --prefix landing install && npm --prefix landing run build && npm install",
  "outputDirectory": "landing/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
