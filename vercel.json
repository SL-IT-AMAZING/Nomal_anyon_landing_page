{
  "buildCommand": "cd landing && npm ci && npm run build",
  "outputDirectory": "landing/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
